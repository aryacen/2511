package dungeonmania.util;

public class EntityConstants {
    // This constant is used to denote that item is not on the map / in the players inventory
    public static Position notOnMap = new Position(-1, -1);
}
